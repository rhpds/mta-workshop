= AI-Powered Application Modernization with migration coding assistant for MTA - Instructions
:imagesdir: ../assets/images/

== 1. Goals of this lab

Experience end-to-end application modernization with **Red Hat migration coding assistant for MTA** in **Red Hat OpenShift Dev Spaces**: use the **MTA extension** for static analysis and **migration coding assistant** (generative AI) to move legacy **Java EE** coolstore code toward **Quarkus** and **Jakarta EE**, and understand how analysis, AI-assisted fixes, and the **Solution Server** fit together on the path to OpenShift.

As enterprise organizations accelerate their cloud adoption journey, one of the most significant challenges they face is modernizing legacy Java applications to run on modern platforms like Red Hat OpenShift. The traditional approach to code refactoring is time-consuming, error-prone, and requires deep knowledge of both legacy and target technologies.

The goal of this lab is to experience how **Red Hat migration coding assistant for Migration Toolkit for Applications (MTA)** revolutionizes application modernization by combining static code analysis with generative AI to accelerate the migration of JavaEE 7 applications to **Quarkus**.

This hands-on lab covers the following topics:

* Configure the MTA extension in Red Hat Dev Spaces, including migration coding assistant (AI-assisted migration)
* Analyze the application and receive AI-powered code refactoring recommendations
* Apply intelligent fixes and migrate the application to Quarkus and Jakarta EE.
* Deploy and validate the modernized application on OpenShift

== 2. Introduction to migration coding assistant for MTA

Migration Toolkit for Applications has long been the industry-leading solution for assessing and analyzing applications for cloud migration. **migration coding assistant** is MTA's **AI-powered code refactoring** feature—not a separate extension. It brings generative AI-assisted migration into the developer's IDE, transforming how organizations approach application modernization.

=== 2.1. The Challenge of Application Modernization

Enterprises face several critical challenges when modernizing their application portfolios:

* **Security concerns** - Outdated applications and frameworks introduce significant risks to business operations, potentially leading to data breaches and compliance violations.
* **Complexity of legacy systems** - Legacy codebases often lack compatibility with modern platforms, have tightly coupled dependencies, and may be unfamiliar to current development teams.
* **Competing priorities** - Organizations must balance the urgency of modernization with limited resources, often prioritizing new features over improving existing applications.
* **Scale and consistency** - Large enterprises may have hundreds of applications requiring similar changes, making manual migration impractical.

=== 2.2. How migration coding assistant Transforms Modernization

**Red Hat migration coding assistant for MTA** denotes MTA modernization that uses both static analysis and **migration coding assistant** (generative AI). In the IDE, the **MTA extension** for Visual Studio Code provides MTA static analysis and migration coding assistant together. That combination addresses these challenges:

* **Speed up application modernization with GenAI** - The MTA extension applies MTA's proven static code analysis together with migration coding assistant generative AI to identify migration issues and propose context-aware solutions. The AI generates tailored code recommendations directly within the IDE, eliminating context switching and keeping developers focused on their work. The system handles ripple effects of code changes automatically, maintaining code integrity throughout the refactoring process. Additionally, it tracks migration metrics and learns from previous solutions to continuously improve recommendations over time.


* **Get helpful refactored code suggestions developers trust** - The system uses rule-based analysis to guide the LLM on what migration issues to look for and how to solve them, ensuring recommendations are grounded in proven migration patterns. Every suggestion includes clear explanations of what needs to change and why, helping developers understand the reasoning behind each proposed fix. Real-time IDE diagnostic data ensures fixes address actual compilation and runtime issues rather than hypothetical problems. Throughout the process, developers review and approve changes, maintaining full control over the refactoring process and ensuring code quality.


* **Modernize custom and proprietary technologies** - migration coding assistant enables creating custom rules that specify conditions and actions for organization-specific frameworks, allowing the AI to understand and work with a unique technology stack. This capability automates code transformation for applications using proprietary or corporate libraries that aren't covered by standard migration rules. Extend the out-of-the-box migration paths with organizational knowledge, making the tool adaptable to specific business needs.


* **Scale modernization across the portfolio** - migration coding assistant learns from changes made during the migration process, building institutional knowledge that improves over time. As it encounters similar patterns across applications, it accurately predicts and generates solutions for similar applications across the portfolio. This learning capability enables refactoring multiple applications predictably and efficiently with consistent quality, making portfolio-scale modernization achievable.


* **Seamless integration into development workflows** - All analysis and refactoring happens directly in Red Hat Devspaces, keeping developers in their familiar environment. Detailed issues, proposed solutions, and code diffs are visible without switching contexts or jumping between different tools. Once refactoring is complete, push the refactored code through existing CI/CD pipelines for automated testing and deployment, ensuring the modernized application integrates seamlessly with established development processes.


* **Choose an AI model** - migration coding assistant supports using a custom LLM—public or self-hosted—with no fine-tuning required, eliminating vendor lock-in. The RAG architecture learns from past solutions and stores them in a knowledge base, continuously improving its recommendations based on the organization's migration history. The system is compatible with multiple LLM providers through OpenAI-compatible APIs, including Red Hat OpenShift AI (enterprise model builder & inference server), Red Hat Enterprise Linux AI (enterprise inference server), and vLLM (popular enterprise inference server). This flexibility supports balancing performance, cost, and data privacy according to specific requirements.


== lab 1: Access DevSpaces and configure the MTA extension (migration coding assistant for MTA)

[IMPORTANT]
====
In this lab, we will use the **Migration Toolkit for Applications** Visual Studio Code extension—the **MTA extension** in a Red Hat DevSpaces environment that provides a VSCode-based GUI.

**migration coding assistant** is not a separate extension. It is MTA's AI-powered code refactoring feature. The **MTA extension** is what is installed in the IDE; it includes both MTA static code analysis and migration coding assistant (generative AI).
====

[.goal]
.Goal
[NOTE]
====
Prepare the **OpenShift Dev Spaces** workspace and **MTA extension** for the rest of the lab: open the **migration coding assistant-coolstore** project, create a **migration profile** (source and target technologies), and connect **migration coding assistant** to the LLM via **GenAI** / **provider-settings.yaml** so you can analyze and modernize ACME’s applications with AI assistance.
====

We will start by setting up the development environment with AI capabilities to start modernizing ACME's coolstore applications.

Prepare to launch DevSpaces and configure the MTA extension, including migration coding assistant (AI) settings.

. Switch to Red Hat OpenShift DevSpaces by clicking the **OpenShift DevSpaces** tab in the workshop interface.  

. Log in to Dev Spaces using the OpenShift credentials:
+
NOTE: The initial page is the Keycloak SSO login page
+
* Username: `{user}`
* Password: `{password}`

. After logging in, if prompted to __Authorize Access__
+
* Click **Allow selected permissions** button
+
image::mta-dls/login-devspaces.jpg[DevSpaces Login,link=self,window=blank,title="Logging In"]
+
CAUTION: If a message indicates authentication is required again, click the **OpenShift DevSpaces** tab to return to the workspace listing.

. Open the user-{user}-workspace 
+
* After logging in the workspace listing opens and shows the user-{user}-workspace entry
* Click on the **Open** link on the right side of workspace listing
+
image::mta-dls/workspace-listing.jpg[Open Workspace,link=self,window=blank,title="Open Workspace"]

. Wait for the workspace to initialize and open:
+
The **migration coding assistant-coolstore** gitea repository loads in DevSpaces and the development environment is ready.
+
The workspace opens in a new browser tab. 
+
* If prompted to trust the content and providers, confirm trust.
* If an introductory layout panel appears, choose **Light** theme for best contrast later in the lab and **Mark Done**. Mark done will close the introductory panel and open the extensions installed panel by clicking on the extensions icon on the left side of the screen as shown in Step 2 in the image below.
+
image::mta-dls/mca-01.png[VSCode Tab,link=self,window=blank,title="Mark done"]

In some cases the extensions might take some time to install. If you see the reload window button then click on it. It will reload the browser tab automatically. 

image::mta-dls/mca-02.png[VSCode Tab,link=self,window=blank,title="Trus the Authors"]

If you see the message "Trust the Authors" then click on the "Trust the Authors" button. This will trust the Red Hat Authors of the extensions and allow you to use the extensions. This is standard VS Code behaviour.

image::mta-dls/mca-03.png[VSCode Tab,link=self,window=blank,title="Manage profiles"]

Managing multiple profiles is a powerful feature of the MTA extension. It allows you to switch between different analysis profiles easily. You can create a new profile by clicking on the "New Profile" button as shown in Step 3 in the image below. We have already created two profiles one with custom rules `audit-logging` and the other `quarkus-profile`. to migration our applicaiton to Quarkus. Click on the `Manage Profiles` button to open the profiles management panel.

image::mta-dls/mca-04.png[VSCode Tab,link=self,window=blank,title="VSCode Tab"]

Review the profiles and select the `quarkus-profile` profile by clicking on the profile name.

image::mta-dls/mca-05.png[VSCode Tab,link=self,window=blank,title="Review profiles"]

Click on the "Activate Profile" button to activate the profile. Once done, click on the right panel again to open the provider settings configuration. As shown in the image below.

image::mta-dls/mca-06.png[VSCode Tab,link=self,window=blank,title="Gen-AI Provider Settings"]

This should open the provider settings configuration. MTA uses the provider settings to connect to the LLM. To configure the LLM, replace the following configuration with your API key `{litellm_virtual_key}` and base URL `{litellm_api_base_url}`. 

[.console-input]
[source,yaml,subs="+attributes,macros+"]
----
models:
  OpenShift: &active
    environment:
      OPENAI_API_KEY: "$KEY$"
    provider: ChatOpenAI
    args:
      model: "llama-scout-17b"
      configuration:
        baseURL: "$BASE_URL$"
----

image::mta-dls/mca-07.png[VSCode Tab,link=self,window=blank,title="VSCode Tab"]


=== Verify (lab 1)

Confirm the DevSpaces environment and MTA extension (including migration coding assistant) are set up correctly



. Confirm MTA Extension is setup correctly
* With the MTA extension icon selected and the MTA Analysis View tab active:
* Click on the gear icon on the right side of the screen
* The **Get Ready to Analyze** pane will open on the right hand side of the screen
* There should be green check marks in all four of the message boxes and each should be listed as completed

. Confirm the DevSpaces workspace is pointing to the correct gitea repository 
* Click on the file icon on the left side of the DevSpaces environment
* The File _EXPLORER_ lists the **migration coding assistant-coolstore** directory
* Expand directories with the file explorer arrows if needed


== Modernize Message-Driven Bean (MDB) to Reactive Messaging

[.goal]
.Goal
[NOTE]
====
Use **MTA** analysis and **migration coding assistant** to modernize a legacy **Message-Driven Bean** (`OrderServiceMDB`) from **Java EE / JMS**-style processing toward **Quarkus** with **SmallRye Reactive Messaging** (`@Incoming`, CDI), and review an AI-generated patch before accepting changes.
====

Modernizing legacy "Enterprise Java" (Java EE/Jakarta EE) applications is often a manual, error-prone process. This lab guide focuses on **migration coding assistant**—the AI feature of MTA, delivered through the MTA extension—which combines static code analysis with Large Language Models (LLMs) to automate the refactoring of legacy patterns into modern, cloud-native **Quarkus** code.

=== Modernize Message-Driven Bean (MDB) to Reactive Messaging
In this lab, let's transform a legacy `OrderServiceMDB` into a modern Quarkus service using SmallRye Reactive Messaging.

==== The Architectural Shift: Synchronous vs. Asynchronous
Legacy Java EE relies on **JMS (Java Message Service)**. While technically asynchronous, JMS in an MDB context often operates in a "blocking" mindset. The MDB container requires heavy resource-consuming threads to sit and wait for messages to arrive.

**Reactive Messaging** (via brokers like Kafka) is built on the **Reactive Streams** standard. Instead of a thread "waiting," the system uses a non-blocking stream. When a message arrives at a Kafka topic, it is "pushed" through a functional pipeline.

==== Why Cloud-Native?
* **Resource Efficiency:** Thousands of messages are handled with only a few CPU threads.
* **Elastic Scaling:** Reactive services scale up and down rapidly in Kubernetes because they lack the "baggage" of an EJB container.
* **Developer Experience:** Quarkus replaces dozens of lines of XML or complex `@ActivationConfigProperty` with a simple `@Incoming` annotation.

==== Step-by-Step with migration coding assistant
*1*. The first step is to review the Analysis from our previous lab.
In the `Analysis panel` search box type the class name `OrderServiceMDB`.

image::mta-dls/orderservicemdb-analysis-results.jpg[OrderServiceMDB,link=self,window=blank,title="OrderServiceMDB"]

Let's review the issues the static code analyzer has reported. The issues are reported in a list as shown in the image above. Click on the `>` icon to get more details. You should be able to see the following issues reported.

- `@MessageDrivenBean` - EJBs are not supported in Quarkus.
  Enterprise Java Beans (EJBs) are not supported in Quarkus. CDI must be used. Please replace the @MessageDriven annotation with a CDI scope annotation like @ApplicationScoped.
- Configure message listener method with `@Incoming`. 
  The destinationLookup property can be migrated by annotating a message handler method (potentially onMessage) with the org.eclipse.microprofile.reactive.messaging.Incoming annotation, indicating the name of the queue as a value. For example:
[.console-input]
[source,java,subs="+attributes,macros+"]
  ----
  // Modernized Quarkus Code
  @ApplicationScoped
  public class OrderService {
      @Incoming("orders") 
      public void process(Order order) {
          // Business logic here
      }
  }
  ----
- JMS is not supported in Quarkus.
  References to JavaEE/JakartaEE JMS elements should be removed and replaced with their Quarkus SmallRye/Microprofile equivalents.
- The package `javax` has been replaced by `jakarta`.
  Replace the javax.jms import statement with jakarta.jms

*2*. Now that we understand the issues of JavaEE to Quarkus migration in context of messaging, lets ask the LLM to generate curated fixes for this migration scenario.

migration coding assistant can request fix for a single issue, but it can also request a fix for the entire file. Click on the drop down menu as shown in the image below, and select `Files` option. This will group all the issues together. 

image::mta-dls/orderservicemdb-analysis-selection.jpg[OrderServiceMDB,link=self,window=blank,title="OrderServiceMDB"]

Next click on the `AI` dropdown as shown in the image below and click `Get Solution`. This will trigger a new tab that will now send the entire context to the LLM. The context includes the issue itself, but it also includes more details like examples, and whats expected as a fix. This approach helps us to engineer the context in a way which is more specific to the requirements. A chat-bot would usually be a free flow of questions and messages. Here the context is prepared by migration coding assistant based on the static code analysis, in order to get the best results without wasting time or tokens. 

image::mta-dls/orderservicemdb-get-solution.jpg[OrderServiceMDB,link=self,window=blank,title="OrderServiceMDB"]

The results start to appear in our `MTA Resolution Details` tab. You can note that the LLM provides additional information that is useful for developers, it describes the chain of thought, and finally sends back a git patch for us to review. At this moment we have 3 options, "Review", "Accept" and "Reject". Lets review by clicking `Review in Editor`, which should open up the editor window with details about the code. Notice that the `Red` highlight is for the code that has been removed and the `Green` highlight is for the code that has been added by migration coding assistant.

image::mta-dls/orderservicemdb-generated-results.jpg[OrderServiceMDB,link=self,window=blank,title="OrderServiceMDB"]

Let's review the changes made. 

All javax. imports are remove. And you can see that it has added the `ApplicationScoped` for the bean and `Incoming` for reactive messaging using microprofile.
[.console-input]
[source,java,subs="+attributes,macros+"]
----
import jakarta.inject.Inject;
import org.eclipse.microprofile.reactive.messaging.Incoming;
import javax.enterprise.context.ApplicationScoped;
----

Next the following JMS lines are removed, as its no longer used.
[.console-input]
[source,java,subs="+attributes,macros+"]
----
@MessageDriven(name = "OrderServiceMDB", activationConfig = {
	@ActivationConfigProperty(propertyName = "destinationLookup", propertyValue = "topic/orders"),
	@ActivationConfigProperty(propertyName = "destinationType", propertyValue = "javax.jms.Topic"),
	@ActivationConfigProperty(propertyName = "acknowledgeMode", propertyValue = "Auto-acknowledge")})
----

The following code is added. Note the use of `Incoming` annotation, which has the `topic/orders`. The topic is chosen based on the context of the code plus the guidance from migration coding assistant.

[.console-input]
[source,java,subs="+attributes,macros+"]
----
    @Incoming("topic/orders")
    public void onMessage(String orderStr) {
        System.out.println("\nMessage recd !");
        try {
            System.out.println("Received order: " + orderStr);
            Order order = Transformers.jsonToOrder(orderStr);
            System.out.println("Order object is " + order);
            orderService.save(order);
            order.getItemList().forEach(orderItem -> {
                catalogService.updateInventoryItems(orderItem.getProductId(), orderItem.getQuantity());
            });
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
----
Following image shows the entire patch generated by migration coding assistant. You can click on the image to get a larger view.

image::mta-dls/orderservicemdb-git-patch.jpg[OrderServiceMDB,link=self,window=blank,title="OrderServiceMDB"]

=== What we learnt from this lab

* **Concepts:** How legacy JMS and MDB-style messaging differs from SmallRye Reactive Messaging on Quarkus—reactive streams, non-blocking handling, and typical cloud-native benefits (fewer threads, easier scaling, simpler code than heavy EJB and JMS configuration).
* **Tooling:** How migration coding assistant in the MTA extension pairs static analysis with an LLM to suggest concrete refactors from Java EE and Jakarta EE patterns toward Quarkus.
* **Analysis skills:** How to find a class (`OrderServiceMDB`) in the Analysis panel and interpret findings—for example that EJBs and `@MessageDriven` are not supported, that you should use `@Incoming`, that JMS should give way to MicroProfile Reactive Messaging, and that `javax` imports move to `jakarta`.
* **Workflow:** How to group issues by file, use **Get Solution**, and read results in **MTA Resolution Details**—including explanation and a git-style patch—with **Review**, **Accept**, **Reject**, and **Review in Editor** (removed code highlighted in red, added code in green).
* **Outcome:** A worked example of modernizing the MDB: `@ApplicationScoped`, `@Incoming` for the channel (for example `topic/orders`), removing `@MessageDriven`, activation configuration, and JMS-centric setup, while preserving the core message-handling logic—so you have seen analysis → AI-generated fix → reviewable patch end to end.


== lab 4: Transforming Legacy EJBs to REST Endpoints

[.goal]
.Goal
[NOTE]
====
Use **migration coding assistant** to refactor the `ShippingService` **EJB** toward **Quarkus**-friendly **CDI** and **HTTP**-oriented design, work through **Get Solution** and **MTA Resolution Details**, and optionally use **agent**-assisted follow-up to address related build and configuration issues.
====

Enterprise JavaBeans (EJBs) were a popular way to build enterprise applications in the past. However, they are not supported in Quarkus. In this lab we will transform a `ShippingService` from a Stateless Session Bean (EJB) into a modern RESTful API. 

=== The Paradigm Shift: RMI to HTTP
Legacy EJBs were designed for RMI (Remote Method Invocation). This required brittle "Remote Interfaces" and tight coupling between the client and the server.

REST (Representational State Transfer) uses standard HTTP. It is the universal language of the cloud, allowing any service (Python, Go, JS) to communicate with the Java backend without special client libraries. This is a key benefit of cloud-native architecture.

==== Benefits of Going Cloud-Native
* **Decoupling:** Services are found via URLs, not complex JNDI lookups.
* **Observability:** Modern tools like Service Meshes (Istio) can monitor HTTP traffic easily; RMI is largely "invisible" to cloud infrastructure.
* **Simplicity:** Refactoring moves from the complex EJB lifecycle to a simple `@ApplicationScoped` CDI bean.


==== Step-by-Step with migration coding assistant
*1*. The first step is to review the Analysis from our previous lab. In the Analysis panel search box type the class name `ShippingService`. Before we start, ensure that the `Agent Mode` toggle is set to `On`. 

image::mta-dls/shippingservice-analysis-results.jpg[ShippingService,link=self,window=blank,title="ShippingService"]

Lets review the issues reported.
- `@Stateless` and `@Remote` annotations are not supported in Quarkus.
  Enterprise Java Beans (EJBs) are not supported in Quarkus. CDI must be used. Please replace the @Stateless annotation with a CDI scope annotation like @ApplicationScoped.
- `javax.ejb` imports are not supported in Quarkus.
  Replace the javax.ejb import statement with jakarta.ejb
- The package `javax` has been replaced by `jakarta`.
  Replace the javax.jms import statement with jakarta.jms


*2*. Now that we understand the issues of JavaEE to Quarkus migration in context of messaging, lets ask the LLM to generate curated fixes for this migration scenario.

migration coding assistant can request fix for a single issue, but it can also request a fix for the entire file. Click on the drop down menu as shown in the image below, and select Files option. This will group all the issues together.

image::mta-dls/shippingservice-analysis-results.jpg[ShippingService,link=self,window=blank,title="ShippingService"]

Next click on the `AI` dropdown as shown in the image below and click `Get Solution`. This will trigger a new tab that will now send the entire context to the LLM. The context includes the issue itself, but it also includes more details like examples, and whats expected as a fix. This approach helps us to engineer the context in a way which is more specific to the requirements. A chat-bot would usually be a free flow of questions and messages. Here the context is prepared by migration coding assistant based on the static code analysis, in order to get the best results without wasting time or tokens. 

image::mta-dls/shippingservice-get-solution.jpg[ShippingService,link=self,window=blank,title="ShippingService"]

Just like in the previous lab the results start to appear in our MTA Resolution Details tab. You can note that the LLM provides additional information that is useful for developers, it describes the chain of thought, and finally sends back a git patch for us to review. At this moment we have 3 options, "Review", "Accept" and "Reject". 

image::mta-dls/shippingservice-generated-results.jpg[ShippingService,link=self,window=blank,title="ShippingService"]

At this point press the "Accept button". And you should see the following message in the `MTA Resolution Details` tab, that is asking you to fix more issues.

image::mta-dls/shippingservice-fix-more-issues.jpg[ShippingService,link=self,window=blank,title="ShippingService"]

We will now fix the remaining issues by clicking on the "Yes" button. 

image::mta-dls/dls-agents.jpg[ShippingService,link=self,window=blank,title="ShippingService"]

Agents will look to fix potential issues once a fix was made. 
For example a Fix was made to move OrderServiceMDB to Reactive Messaging. What it didnt change is the pom.xml or application.properties. Or other dependencies tht could be affected. Agent will fix all of those. The agent understands the context and the issues that arise from that fix. It uses tooling like the LSP server, build tools, compilation etc. 

Agent will also keep on fixing issues as it finds them, at each step it will ask if the user would like to fix potential issues found in the process. User can always quit at any stage. 

[IMPORTANT]
====
Please try out some of the fixes from the agents, and feel free to press "No" if you dont want to continue with the agent changes. You can also accept or reject fixes suggested by the agent as you see fit.
====
